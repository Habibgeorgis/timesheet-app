"use client";
import { useActionState, useState } from "react";
import { LoaderCircle, Pencil, X } from "lucide-react";
import { saveEntry } from "@/actions/timesheets";
import { DurationInput } from "@/components/duration-input";

export function EditEntryButton({entryId,timesheetId,date,minutes}:{entryId:string;timesheetId:string;date:string;minutes:number}) {const [open,setOpen]=useState(false);const [state,action,pending]=useActionState(saveEntry,{});return <><button type="button" onClick={()=>setOpen(true)} title="Edit entry" aria-label="Edit entry" className="grid size-9 place-items-center text-[#52615d] hover:bg-[#eef4f2]"><Pencil size={16}/></button>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/30 p-4" onMouseDown={()=>setOpen(false)}><form action={action} onMouseDown={event=>event.stopPropagation()} className="panel w-full max-w-[380px] p-5 shadow-xl"><div className="flex items-start justify-between"><div><h2 className="font-bold">Edit tracked time</h2><p className="mt-1 text-sm text-[#66736f]">Update the hours and minutes.</p></div><button type="button" onClick={()=>setOpen(false)} className="grid size-9 place-items-center rounded hover:bg-[#f2f5f4]" aria-label="Close"><X size={18}/></button></div>{state.error&&<div className="mt-4 rounded-md bg-red-50 px-3 py-2 text-sm text-red-700">{state.error}</div>}{state.success&&<div className="mt-4 rounded-md bg-emerald-50 px-3 py-2 text-sm text-emerald-700">{state.success}</div>}<input type="hidden" name="entryId" value={entryId}/><input type="hidden" name="timesheetId" value={timesheetId}/><input type="hidden" name="date" value={date}/><div className="mt-5"><span className="label">Hours : Minutes</span><DurationInput defaultMinutes={minutes} idPrefix={`edit-${entryId}`}/></div><button disabled={pending} className="btn btn-primary mt-5 w-full">{pending?<LoaderCircle size={18} className="animate-spin"/>:<Pencil size={17}/>}Save changes</button></form></div>}</>}
